import SettingsSheet from "../../features/settings/SettingsSheet";

export default function AppShell() {
  // Aktuell nicht genutzt – dient nur dazu, TypeScript-Fehler zu vermeiden.
  // Wenn du AppShell wieder brauchst, kannst du hier die echte Shell zusammenbauen.
  return null;
}
